// src/runtime-config.js
(function(window) {
    window.__env = window.__env || {};
    window.__env.admin = '${Admin}'; // Placeholder for Admin variable
    window.__env.developer = '${Developer}'; // Placeholder for Developer variable
  }(this));
  